<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Property for log file location -->
    <property name="LOG_PATH" value="${LOG_PATH:-logs}"/>
    <property name="APP_NAME" value="nested-api"/>
    
    <!-- 
        Exception pattern that keeps stack trace on single line for Grafana/Loki
        %replace replaces newlines with | so entire exception is one log entry
    -->
    <property name="EXCEPTION_PATTERN" value="%replace(%xException{full}){'\n', ' | '}%nopex"/>

    <!-- Console Appender with colored output and trace ID (multiline for readability) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%X{traceId:-NO_TRACE}] [%X{spanId:-}] %cyan(%-40.40logger{39}) : %msg%n%xException{full}</pattern>
        </encoder>
    </appender>

    <!-- File Appender - Exceptions on single line for log aggregation -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${APP_NAME}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>50MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%X{traceId:-NO_TRACE}] [%X{spanId:-}] [%thread] %logger{50} : %msg ${EXCEPTION_PATTERN}%n</pattern>
        </encoder>
    </appender>
    
    <!-- JSON Appender for Grafana/Loki - Best for log aggregation -->
    <appender name="JSON_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}-json.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${APP_NAME}-json.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>
            <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSSZ</timestampPattern>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <message>message</message>
                <logger>logger</logger>
                <thread>thread</thread>
                <level>level</level>
                <stackTrace>stackTrace</stackTrace>
            </fieldNames>
        </encoder>
    </appender>

    <!-- Scheduler-specific appender -->
    <appender name="SCHEDULER_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/scheduler.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/scheduler.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>14</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%X{traceId:-NO_TRACE}] [%X{spanId:-}] %logger{30} : %msg ${EXCEPTION_PATTERN}%n</pattern>
        </encoder>
    </appender>

    <!-- Development profile - more verbose console output -->
    <springProfile name="dev,local">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>

        <!-- Scheduler logs go to separate file too -->
        <logger name="com.nested.app.services.FundSyncScheduler" level="DEBUG" additivity="true">
            <appender-ref ref="SCHEDULER_FILE"/>
        </logger>

        <!-- More SQL debugging in dev -->
        <logger name="org.hibernate.SQL" level="DEBUG"/>
        <logger name="org.hibernate.orm.jdbc.bind" level="TRACE"/>
    </springProfile>

    <!-- Production profile - JSON logging for Grafana/Loki -->
    <springProfile name="prod,production">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="JSON_FILE"/>
        </root>

        <logger name="com.nested.app.services.FundSyncScheduler" level="INFO" additivity="true">
            <appender-ref ref="SCHEDULER_FILE"/>
        </logger>

        <!-- Reduce noise in production -->
        <logger name="org.springframework.web" level="WARN"/>
        <logger name="org.springframework.security" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
    </springProfile>

    <!-- Default (if no profile matched) -->
    <springProfile name="default">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>

    <!-- Application-specific loggers -->
    <logger name="com.nested.app" level="DEBUG"/>
    <logger name="com.nested.app.client" level="DEBUG"/>
</configuration>
