package com.nested.app.listeners;

// Deprecated listener removed: OrderStatusCheckJob now creates transactions post-success.
// Keeping class empty (or could delete file) to avoid duplicate processing.
public class OrderItemsTransactionListener {}
